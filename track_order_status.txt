class Product {
    name = "item";
    destination = "miles";
    time = "minutes";

    constructor(name, destination, time) {
      this.name = name;
      this.destination = destination;
      this.time = time;
    }
  }
import "Status updates: placed, preparing, dispatched, delivered";
import "Integration with robot tracking for dispatched status";
import "real-time updates with timestamps and notifications";
